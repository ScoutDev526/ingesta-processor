spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  mail:
    host: localhost
    port: 1025

ingesta:
  jobs:
    path: classpath:jobs
  working-directory: /tmp/ingesta-test
  notification:
    enabled: false
  scheduler:
    enabled: false
