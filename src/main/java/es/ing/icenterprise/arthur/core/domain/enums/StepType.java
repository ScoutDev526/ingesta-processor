package es.ing.icenterprise.arthur.core.domain.enums;

public enum StepType {
    // Transformation types
    TRIM,
    UPPERCASE,
    CONCATENATE,
    // Persistence types
    SELECT,
    INSERT,
    TRUNCATE
}
